# Database
DATABASE_URL="file:./dev.db"

# Email Configuration for Local Development
# Choose one of the following email services:

# Option 1: Gmail (for real email testing - requires app password)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Option 2: Mailtrap (professional email testing service)
# EMAIL_SERVICE=mailtrap
# MAILTRAP_USER=your-mailtrap-username
# MAILTRAP_PASS=your-mailtrap-password

# Option 3: Ethereal Email (default - creates fake accounts, NO REAL EMAILS)
# EMAIL_SERVICE=ethereal
# (No additional config needed - emails are NOT sent, only preview URLs)

# Email sender information
EMAIL_FROM="Medieval Commanders <noreply@medievalcommanders.com>"

# Default admin email (will be used if no admin is found in database)
DEFAULT_ADMIN_EMAIL="admin@medievalcommanders.com"

# IMPORTANT NOTES:
# - Ethereal Email: Creates fake accounts, emails are NOT sent (test only)
# - Gmail: Sends real emails (requires app password setup)
# - Mailtrap: Professional testing service, emails go to test inbox
# - For real email testing, use Gmail with app password
# - For production, use SendGrid, AWS SES, or other email services