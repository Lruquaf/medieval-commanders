# Database
DATABASE_URL="your-production-database-url"

# Email Configuration for Production
# Choose one of the following email services:

# Option 1: SendGrid (Recommended - High deliverability)
EMAIL_SERVICE=sendgrid
SENDGRID_API_KEY=your-sendgrid-api-key

# Option 2: AWS SES (Cost-effective for AWS deployments)
# EMAIL_SERVICE=ses
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Option 3: Mailgun (Reliable email service)
# EMAIL_SERVICE=mailgun
# MAILGUN_API_KEY=your-mailgun-api-key
# MAILGUN_DOMAIN=your-mailgun-domain

# Option 4: Custom SMTP (Your own SMTP server)
# EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password

# Email sender information
EMAIL_FROM="Medieval Commanders <noreply@medievalcommanders.com>"

# Default admin email (will be used if no admin is found in database)
DEFAULT_ADMIN_EMAIL="admin@medievalcommanders.com"

# PRODUCTION SETUP NOTES:
# 1. SendGrid: Free tier available (100 emails/day), easy setup
# 2. AWS SES: Very cost-effective, requires AWS account
# 3. Mailgun: Good deliverability, free tier available
# 4. Custom SMTP: Use your existing email infrastructure
# 5. Test email delivery before going live
# 6. Monitor email delivery rates and bounce rates
# 7. Set up proper SPF, DKIM, and DMARC records for your domain
