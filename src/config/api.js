// Deprecated: moved to src/api/client.js
// Keeping file minimal to avoid import crashes if any leftover references exist.
export { default } from '../api/client';
export { uploadClient } from '../api/client';
